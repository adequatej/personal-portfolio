{
  "rules": {
    "visitorCount": {
      ".read": true,
      ".write": "newData.val() > data.val() || !data.exists()"
    },
    "contactMessages": {
      ".read": false,
      ".write": true
    }
  }
}